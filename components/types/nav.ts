import { } from "react";

export interface NavItem {
  title: string;
  href?: string;
  disabled?: boolean;
  external?: boolean;
  // Add any other properties that NavItem should have
}

// Adding an empty export statement
export {};


